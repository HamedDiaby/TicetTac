<%- include('header'); %>

<div class="d-flex flex-column align-items-center trips">
    <div class="col-sm-10 col-md-6">
        <h5 class="text-white">My last trips :</h5>
    </div>
    
    <table class="table table-borderless col-sm-10 col-md-8 col-lg-8">
        <thead class="bg-white">
          <tr>
            <th scope="col">Journey</th>
            <th scope="col">Date</th>
            <th scope="col">Departure time</th>
            <th scope="col">Price</th>
          </tr>
        </thead>
        <tbody class="bg-white">

          <% for(var i=0; i<historic.mylasttrips.length; i++){%>

          <tr>
            <td><%=historic.mylasttrips[i].departure%>/ <%=historic.mylasttrips[i].arrival%></td>
            <td><%=formatDate(historic.mylasttrips[i].date)%></td>
            <td><%=historic.mylasttrips[i].departureTime%></td>
            <td><%=historic.mylasttrips[i].price%> €</td>
          </tr>

         <%}%>

       

        </tbody>
    </table>
</div>

    <a href="/" type="button" class="confirm btn btn-danger">Home</a>

  <%- include('footer'); %>

